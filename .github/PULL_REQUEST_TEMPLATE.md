<!-- Thanks for contributing! Keep the IR small — see CONTRIBUTING.md. -->

## What & why

<!-- What does this change, and what problem does it solve? Link any issue. -->

Closes #

## Checklist

- [ ] `node --check src/*.js plugin/code.js` passes
- [ ] `node src/server.js --version` and `--help` still work
- [ ] `npm publish --dry-run` is clean
- [ ] Ran the README smoke test against a real Figma file
- [ ] If I added an IR field: it maps one-to-one onto a single Figma concept, and `plugin/code.js` handles it
- [ ] Updated the README / docs if behavior changed
